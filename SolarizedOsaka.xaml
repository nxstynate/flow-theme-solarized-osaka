<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:System="clr-namespace:System;assembly=mscorlib">

  <!-- Merge Base -->
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="pack://application:,,,/Themes/Base.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <!-- Layout + Margin -->
  <Thickness x:Key="ResultMargin">0 0 0 4</Thickness>

  <!-- Core Colors -->
  <SolidColorBrush x:Key="ItemSelectedBackgroundColor">#103956</SolidColorBrush>
  <SolidColorBrush x:Key="BorderBrushColor">#586E75</SolidColorBrush>
  <SolidColorBrush x:Key="DefaultForeground">#657B83</SolidColorBrush>

  <!-- Glyphs -->
  <Style x:Key="ItemGlyph"
         BasedOn="{StaticResource BaseGlyphStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#268BD2" />
  </Style>

  <!-- Query Box -->
  <Style x:Key="QueryBoxStyle"
         BasedOn="{StaticResource BaseQueryBoxStyle}"
         TargetType="{x:Type TextBox}">
    <Setter Property="Foreground" Value="#494FB6" />
    <Setter Property="CaretBrush" Value="#494FB6" />
    <Setter Property="SelectionBrush" Value="#103956" />
  </Style>

  <Style x:Key="QuerySuggestionBoxStyle"
         BasedOn="{StaticResource BaseQuerySuggestionBoxStyle}"
         TargetType="{x:Type TextBox}">
    <Setter Property="Foreground" Value="#B58900" />
  </Style>

  <!-- Window Background + Borders -->
  <Style x:Key="WindowBorderStyle"
         BasedOn="{StaticResource BaseWindowBorderStyle}"
         TargetType="{x:Type Border}">
    <Setter Property="Background" Value="#101010" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}" />
    <Setter Property="CornerRadius" Value="6" />
  </Style>

  <!-- Window Wrapper (Increased Width) -->
  <Style x:Key="WindowStyle"
         BasedOn="{StaticResource BaseWindowStyle}"
         TargetType="{x:Type Window}">
    <Setter Property="MinWidth" Value="1920" />
  </Style>

  <!-- Separator Line -->
  <Style x:Key="SeparatorStyle"
         BasedOn="{StaticResource BaseSeparatorStyle}"
         TargetType="{x:Type Rectangle}">
    <Setter Property="Fill" Value="#000000" />
    <Setter Property="Height" Value="1" />
    <Setter Property="Margin" Value="12 0 12 4" />
  </Style>

  <!-- Pending Line -->
  <Style x:Key="PendingLineStyle"
         BasedOn="{StaticResource BasePendingLineStyle}"
         TargetType="{x:Type Line}">
    <Setter Property="Stroke" Value="#494FB6" />
  </Style>

  <!-- Result Item Title -->
  <Style x:Key="ItemTitleStyle"
         BasedOn="{StaticResource BaseItemTitleStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#657B83" />
  </Style>

  <Style x:Key="ItemSubTitleStyle"
         BasedOn="{StaticResource BaseItemSubTitleStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#9EACAD" />
    <Setter Property="Opacity" Value="0.7" />
  </Style>

  <Style x:Key="ItemTitleSelectedStyle"
         BasedOn="{StaticResource BaseItemTitleSelectedStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#EEE8D5" />
  </Style>

  <Style x:Key="ItemSubTitleSelectedStyle"
         BasedOn="{StaticResource BaseItemSubTitleSelectedStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#859900" />
  </Style>

  <!-- Hotkey Styles -->
  <Style x:Key="ItemHotkeyStyle"
         BasedOn="{StaticResource BaseItemHotkeyStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#859900" />
  </Style>

  <Style x:Key="ItemHotkeySelectedStyle"
         BasedOn="{StaticResource BaseItemHotkeySelectedStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#859900" />
  </Style>

  <!-- Thumb/Scroll -->
  <Style x:Key="ThumbStyle"
         BasedOn="{StaticResource BaseThumbStyle}"
         TargetType="{x:Type Thumb}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Border
            Background="#859900"
            BorderBrush="Transparent"
            BorderThickness="0"
            CornerRadius="2"
            DockPanel.Dock="Right" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Preview Panel -->
  <Style x:Key="PreviewBorderStyle"
         BasedOn="{StaticResource BasePreviewBorderStyle}"
         TargetType="{x:Type Border}">
    <Setter Property="BorderBrush" Value="#000000" />
  </Style>

  <Style x:Key="PreviewItemTitleStyle"
         BasedOn="{StaticResource BasePreviewItemTitleStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#f5f5f5" />
  </Style>

  <Style x:Key="PreviewItemSubTitleStyle"
         BasedOn="{StaticResource BasePreviewItemSubTitleStyle}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#c2c2c2" />
  </Style>

  <Style x:Key="PreviewGlyph"
         BasedOn="{StaticResource BasePreviewGlyph}"
         TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="#268BD2" />
  </Style>

  <!-- Hide ScrollBar -->
  <Style TargetType="ScrollBar">
    <Setter Property="Opacity" Value="0" />
    <Setter Property="Width" Value="0" />
    <Setter Property="IsHitTestVisible" Value="False" />
  </Style>

</ResourceDictionary>
